services:
  # FastApi
  api:
    environment:
      - ENV_FILE=envs/.env.prod
    env_file:
      - envs/.env.prod
    ports:
      - "8000:8000"

  # PostgresSQL
  postgres:
    ports:
      - "5432:5432"

  # Redis
  redis:
    ports:
      - "6379:6379"